"use client"
import React from 'react'

const AddToCard = () => {
  return (
    <div>
        <button onClick={() => console.log("Event From Add card")}>Add To Card Click to see Console</button>
    </div>
  )
}

export default AddToCard